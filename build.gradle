task assemble{}

buildscript {
   dependencies {
   classpath 'org.postgresql:postgresql:42.2.5'
   }
}
plugins {
    id "org.flywaydb.flyway" version "5.2.4"
}

flyway {
    url = "jdbc:postgresql://" + System.getenv("HOST_IP_ADDRESS") + ":5432/postgres"
    user = 'postgres'
}
